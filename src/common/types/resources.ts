export enum Resource {
    USERS = "users",
    ROLES = "roles",
    WALLETS = "wallets",
    REQUESTS = "requests",
    TRANSACTIONS = "transactions",
    BANNERS = "banners",
    PLATFORMS = "platforms",
}